<!DOCTYPE html>
<html>
<head>
	<title>Chaosdorf Spendendrucker</title>
	<meta charset="utf-8">
	<style type="text/css">

	body {
		font-family: Sans-Serif;
	}

	div.about {
		margin-top: 2em;
		color: #666666;
	}

	div.about a {
		color: #000066;
		text-decoration: non;
	}

	span.optional {
		color: #666666;
	}

	div.break {
		height: 1em;
	}

	div.field {
		width: 100%;
		clear: both;
	}

	div.field div.desc {
		float: left;
		width: 14em;
		text-align: right;
		padding-right: 0.5em;
	}

	div.field div.desc:after {
		content: ":";
	}

	input, select {
		border: 1px solid black;
	}

	div.notes {
		margin-top: 4em;
	}

	div.notes ul {
		margin-top: 1em;
	}

	</style>
</head>
<body>

<div class="inputs">
%= form_for print => (method => 'POST') => begin
<div class="field">
<div class="desc">Spendenmenge</div>
%= text_field 'amount'
</div>
<div class="field">
<div class="desc">Kontonummer</div>
%= text_field 'konto'
</div>
<div class="field">
<div class="desc">Bankleitzahl</div>
%= text_field 'blz'
</div>
<div class="field">
<div class="desc">Kontoinhaber</div>
%= text_field 'person'
</div>
<div class="field">
<div class="desc">Anschrift</div>
%= text_field 'mail1'
</div>
<div class="field">
<div class="desc">Anschrift</div>
%= text_field 'mail2'
</div>
<div class="field">
<div class="desc">Anschrift</div>
%= text_field 'mail3'
</div>

%= submit_button 'Drucken';

%= end

</div>

</body>
</html>
